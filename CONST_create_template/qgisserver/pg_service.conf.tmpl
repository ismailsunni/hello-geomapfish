[hello_geomapfish]
host=${PGHOST_SLAVE}
port=${PGPORT_SLAVE}
#sslmode=${PGSSLMODE}
user=${PGUSER}
password=${PGPASSWORD}
dbname=${PGDATABASE}

[hello_geomapfish_master]
host=${PGHOST}
port=${PGPORT}
#sslmode=${PGSSLMODE}
user=${PGUSER}
password=${PGPASSWORD}
dbname=${PGDATABASE}
